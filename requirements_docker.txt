# requirements_docker.txt
# Lightweight deps for Docker (CPU only — no torch GPU needed)
# Separate from requirements.txt so we don't install torch on laptop

# Core RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0
hnswlib>=0.8.0

# PDF parsing
pypdf>=4.0.0

# Gemini API
google-genai>=1.0.0

# Live chat UI
gradio>=4.0.0

# Data pipeline
prefect>=3.0.0
feedparser>=6.0.0
requests>=2.31.0

# API server
fastapi>=0.111.0
uvicorn>=0.30.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0
numpy>=1.26.0

# Experiment tracking
wandb>=0.17.0